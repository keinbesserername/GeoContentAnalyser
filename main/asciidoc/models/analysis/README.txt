In diesem Folder sollen die Diagramme gelegt werden, welche für das Pflichtenheft verwendet werden.
Der Pfad wurde bereits im Pflichtenheft angegeben. Somit müssen nur noch die Diagramme in Form eines 
PNG-Files hinzugefügt werden.
Bitte beachte, dass die Diagramme ensprechend benannt sind, da diese sonst nicht ins Pflichtenheft 
eingefügt werden. Wenn mehrere Diagramme der gleichen Art eingefügt werden, dann füge bitte 
entsprechend die Verlinkgung im Pflichtenheft ein.